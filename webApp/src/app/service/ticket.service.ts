import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class TicketService {

  constructor() { }

  page(page: number, size: number, name: any) {
  }

  delete(id: number) {
  }
}
